# API configuration
# In development, usually points to localhost server
VITE_API_URL=http://localhost:4000

# GraphQL path (should match server's GRAPHQL_PATH)
VITE_GRAPHQL_PATH=/graphql

# Any other client-specific environment variables can be added here